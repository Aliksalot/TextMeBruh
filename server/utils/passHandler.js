
// returns encrypted password
const encryptPassword = (password) => {

}

// boolean -> true if are the same, false otherwise
const comparePasswords = (password, hashedPassword)=> {

}

module.exports = {
    comparePasswords,
    encryptPassword
}
